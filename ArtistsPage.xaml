<Page x:Class="SubsonicUWP.ArtistsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SubsonicUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Artists" FontSize="48" FontWeight="Light" Margin="24,12,0,12" FontFamily="Segoe UI Light"/>

        <ScrollViewer Grid.Row="1">
            <GridView ItemsSource="{Binding Artists}" Padding="12" SelectionMode="None" IsItemClickEnabled="False">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,12,24" Width="148" Background="Transparent" Tapped="Artist_Tapped" RightTapped="Artist_RightTapped" Holding="Artist_Holding">
                            <FlyoutBase.AttachedFlyout>
                                <MenuFlyout>
                                    <MenuFlyoutItem Text="Add to current queue" Click="AddToQueue_Click" Icon="Add"/>
                                    <MenuFlyoutItem Text="Play next" Click="PlayNext_Click" Icon="Upload"/>
                                    <MenuFlyoutItem Text="Add to cache" Click="AddToCache_Click" Icon="Download"/>
                                    <MenuFlyoutItem Text="Download" Click="Export_Click" Icon="SaveLocal"/>
                                </MenuFlyout>
                            </FlyoutBase.AttachedFlyout>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="148"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Boxy Artist Placeholder (Square) -->
                            <Border Width="148" Height="148">
                                <Grid>
                                    <!-- Using Contact/Person icon instead of Album -->
                                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.3"/>
                                    <!-- Optional: If we ever get images, bind here -->
                                    <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>
                                    <Border Background="{Binding ColorBrush}" Opacity="0.2"/>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Row="1" Margin="0,8,0,0">
                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" TextAlignment="Left"/>
                                <!-- Optional: 'Artist' label or count if available, otherwise just Name -->
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>

        <!-- Offline Overlay -->
        <Grid Grid.Row="1" Background="{ThemeResource WindowBackgroundBrush}" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Foreground="{ThemeResource AccentBrush}"/>
                <TextBlock Text="Waiting for connection..." Margin="0,12,0,0" HorizontalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
