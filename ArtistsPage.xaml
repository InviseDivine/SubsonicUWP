<Page x:Class="SubsonicUWP.ArtistsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SubsonicUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Artists" FontSize="48" FontWeight="Light" Margin="24,12,0,12" FontFamily="Segoe UI Light"/>

        <ScrollViewer Grid.Row="1">
            <GridView ItemsSource="{Binding Artists}" Padding="12" SelectionMode="None" IsItemClickEnabled="False">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="12" Width="200" Background="Transparent" Tapped="Artist_Tapped">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="200"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Circular Artist Image Mock -->
                            <Border Width="200" Height="200" CornerRadius="100" Background="#333">
                                <Grid>
                                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="64" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.3"/>
                                    <Border Background="{Binding ColorBrush}" CornerRadius="100" Opacity="0.2"/>
                                </Grid>
                            </Border>

                            <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="16" Margin="0,12,0,0" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" TextAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </ScrollViewer>

        <!-- Offline Overlay -->
        <Grid Grid.Row="1" Background="{ThemeResource WindowBackgroundBrush}" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Foreground="{ThemeResource AccentBrush}"/>
                <TextBlock Text="Waiting for connection..." Margin="0,12,0,0" HorizontalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
