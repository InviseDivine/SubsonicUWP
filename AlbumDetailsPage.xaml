<Page x:Class="SubsonicUWP.AlbumDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SubsonicUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Page.Resources>
        <MenuFlyout x:Key="TrackMenu">
            <MenuFlyoutItem Text="Play" Click="TrackPlay_Click" Icon="Play"/>
            <MenuFlyoutItem Text="Play Next" Click="TrackPlayNext_Click" Icon="Upload"/>
            <MenuFlyoutItem Text="Add to Queue" Click="TrackAddToQueue_Click" Icon="Add"/>
            <MenuFlyoutItem Text="Download" Click="TrackDownload_Click" Icon="Download"/>
        </MenuFlyout>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Padding="24" Background="#11FFFFFF">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image x:Name="AlbumCover" Width="120" Height="120" Stretch="UniformToFill" Margin="0,0,24,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="AlbumTitle" Text="Album Name" FontSize="32" FontWeight="SemiBold"/>
                    <TextBlock x:Name="AlbumArtist" Text="Artist" FontSize="18" Opacity="0.7"/>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Play Album" Click="PlayAlbum_Click" Margin="0,0,8,0"/>
                        <Button Content="Download" Click="DownloadAlbum_Click" Margin="0,0,8,0"/>
                        <Button x:Name="PinButton" Content="Pin to Tile" Click="PinButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <ListView x:Name="TrackList" Grid.Row="1" ItemsSource="{Binding Tracks}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="TrackList_ItemClick" Padding="12">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="ContextFlyout" Value="{StaticResource TrackMenu}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,8" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding TrackNumber}" VerticalAlignment="Center" Opacity="0.5" Width="30" TextAlignment="Center"/>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{Binding DurationFormatted}" VerticalAlignment="Center" Opacity="0.6" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Offline Overlay -->
        <Grid Grid.Row="1" Background="{ThemeResource WindowBackgroundBrush}" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Foreground="{ThemeResource AccentBrush}"/>
                <TextBlock Text="Waiting for connection..." Margin="0,12,0,0" HorizontalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
