<Page x:Class="SubsonicUWP.AlbumDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SubsonicUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Page.Resources>
        <MenuFlyout x:Key="TrackMenu">
            <MenuFlyoutItem Text="Play" Click="TrackPlay_Click">
                <MenuFlyoutItem.Icon>
                    <SymbolIcon Symbol="Play"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Play Next" Click="TrackPlayNext_Click">
                <MenuFlyoutItem.Icon>
                    <SymbolIcon Symbol="Upload"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Add to Queue" Click="TrackAddToQueue_Click">
                <MenuFlyoutItem.Icon>
                    <SymbolIcon Symbol="Add"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Add to cache" Click="TrackAddToCache_Click">
                <MenuFlyoutItem.Icon>
                    <SymbolIcon Symbol="Download"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem Text="Export as mp3" Click="TrackExport_Click">
                <MenuFlyoutItem.Icon>
                    <SymbolIcon Symbol="SaveLocal"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
        </MenuFlyout>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="850"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderBorder.Padding" Value="12"/>
                        <!-- AlbumCover Size restored to default (140) to match content height -->
                        <Setter Target="AlbumCover.Margin" Value="0,0,12,0"/>
                        <Setter Target="AlbumTitle.FontSize" Value="20"/>
                        <Setter Target="AlbumArtist.FontSize" Value="14"/>
                        <Setter Target="HeaderButtonsPanel.Margin" Value="0,8,0,0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border x:Name="HeaderBorder" Padding="24" Background="#11FFFFFF">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image x:Name="AlbumCover" Width="140" Height="140" Stretch="UniformToFill" Margin="0,0,24,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="AlbumTitle" Text="Album Name" FontSize="32" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" MaxLines="2"/>
                    <TextBlock x:Name="AlbumArtist" Text="Artist" FontSize="18" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
                    <StackPanel x:Name="HeaderButtonsPanel" Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Click="PlayAlbum_Click" Margin="0,0,8,0" Width="44" Height="44" ToolTipService.ToolTip="Play Album">
                            <SymbolIcon Symbol="Play"/>
                        </Button>
                        <Button Click="AddToCacheAlbum_Click" Margin="0,0,8,0" Width="44" Height="44" ToolTipService.ToolTip="Add to Cache">
                            <SymbolIcon Symbol="Download"/>
                        </Button>
                        <Button Click="ExportAlbum_Click" Margin="0,0,8,0" Width="44" Height="44" ToolTipService.ToolTip="Export as mp3">
                            <SymbolIcon Symbol="SaveLocal"/>
                        </Button>
                        <Button x:Name="PinButton" Click="PinButton_Click" Width="44" Height="44" ToolTipService.ToolTip="Pin to Start">
                            <SymbolIcon Symbol="Pin"/>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <ListView x:Name="TrackList" Grid.Row="1" ItemsSource="{Binding Tracks}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="TrackList_ItemClick" Padding="12">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="ContextFlyout" Value="{StaticResource TrackMenu}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,8" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding TrackNumber}" VerticalAlignment="Center" Opacity="0.5" Width="30" TextAlignment="Center"/>

                                                    
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">

                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{Binding DurationFormatted}" VerticalAlignment="Center" Opacity="0.6" HorizontalAlignment="Right"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Offline Overlay -->
        <Grid Grid.Row="1" Background="{ThemeResource WindowBackgroundBrush}" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Foreground="{ThemeResource AccentBrush}"/>
                <TextBlock Text="Waiting for connection..." Margin="0,12,0,0" HorizontalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
