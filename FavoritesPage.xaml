<Page x:Class="SubsonicUWP.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SubsonicUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Page.Resources>
        <MenuFlyout x:Key="MediaItemMenu">
            <MenuFlyoutItem Text="Add to current queue" Click="AddToQueue_Click" Icon="Add"/>
            <MenuFlyoutItem Text="Add to playlist" Click="AddToPlaylist_Click" Icon="MusicInfo"/>
            <MenuFlyoutItem Text="Play next" Click="PlayNext_Click" Icon="Upload"/>
            <MenuFlyoutItem Text="Favorite" Click="Favorite_Click" Icon="Favorite"/>
            <MenuFlyoutItem Text="Add to cache" Click="AddToCache_Click" Icon="Download"/>
            <MenuFlyoutItem Text="Export as mp3" Click="Export_Click" Icon="SaveLocal"/>
        </MenuFlyout>

        <DataTemplate x:Key="SongTemplate">
            <Grid Width="148" Margin="4, 4, 4, 4" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" FlyoutBase.AttachedFlyout="{StaticResource MediaItemMenu}" RightTapped="MediaItemGrid_RightTapped" Holding="MediaItemGrid_Holding">
                <Grid.RowDefinitions>
                    <RowDefinition Height="148"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="&#xE93C;" FontFamily="Segoe MDL2 Assets" FontSize="40" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5" Visibility="{Binding ImageUrl, Converter={StaticResource NullToVis}}"/>
                <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>

                <Border Grid.Row="1" Padding="4">
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" Foreground="{ThemeResource ForegroundBrush}" TextTrimming="CharacterEllipsis" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Artist}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextTrimming="CharacterEllipsis" FontSize="11"/>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>
        <local:StringNullOrEmptyToVisibilityConverter x:Key="NullToVis"/>
    </Page.Resources>

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="850"/>
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PlayAllText.Visibility" Value="Collapsed"/>
                        <Setter Target="ShuffleText.Visibility" Value="Collapsed"/>
                        <Setter Target="PageTitle.FontSize" Value="32"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="24,12,0,12" VerticalAlignment="Center">
            <TextBlock x:Name="PageTitle" Text="Favorites" FontSize="48" FontWeight="Light" FontFamily="Segoe UI Light" Margin="0,0,24,0"/>

            <Button Background="Transparent" Click="PlayAll_Click" Margin="0,0,12,0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="20" VerticalAlignment="Center"/>
                    <TextBlock x:Name="PlayAllText" Text="Play All" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>

            <Button Background="Transparent" Click="Shuffle_Click" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE8B1;" FontFamily="Segoe MDL2 Assets" FontSize="20" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ShuffleText" Text="Shuffle" Margin="8,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <GridView Grid.Row="1" ItemsSource="{Binding Favorites}" ItemTemplate="{StaticResource SongTemplate}" Padding="12" SelectionMode="None" IsItemClickEnabled="True" ItemClick="GridView_ItemClick">
        </GridView>

        <!-- Offline Overlay -->
        <Grid Grid.Row="1" Background="{ThemeResource WindowBackgroundBrush}" Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Foreground="{ThemeResource AccentBrush}"/>
                <TextBlock Text="Waiting for connection..." Margin="0,12,0,0" HorizontalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
